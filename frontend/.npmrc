# openapi-typescript (codegen-only dev tool) still declares a peer of typescript@^5,
# which conflicts with our TypeScript 6 upgrade even though it runs fine on TS 6.
# Relax peer resolution so `npm ci` / `npm install` don't hard-fail on that stale range.
legacy-peer-deps=true
