# .NET
bin/
obj/
*.user
*.suo
.vs/
*.DotSettings
TestResults/
coverage/

# Build-time OpenAPI document (#197) — regenerated by every `dotnet build`, never a source file;
# the frontend's committed src/api/generated.ts is what's checked against it in CI.
backend/src/ElliotWaveAnalyzer.Api/openapi/

# NuGet
*.nupkg
packages/

# Rider / JetBrains
.idea/
*.sln.iml

# Frontend / Node
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/playwright-report/
frontend/test-results/

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Local dev secrets — never commit real keys or credentials
appsettings.*.json
!appsettings.json

# Local IDE settings (machine-specific paths)
.vscode/

# Verify snapshot tooling — received files are transient
*.received.*

# Stryker mutation testing output (reports/logs are CI artifacts, not source)
StrykerOutput/
